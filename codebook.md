| variables                   | Description                                                                           | Values                                                                                      | Transformation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
|-----------------------------|---------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| ID                          | unique participant ID                                                                 | numeric variable: 1-30 (totally 30 particpants)                                             | 1. raw data are from  two files:  subject_test.txt and subject_train.text; 2. merge data in the step #1                                                                                                                                                                                                                                                                                                                                                                                            |
| Activity                    | 6 types of activities                                                                 | character variable: walking, waling_upstairs, walking_downstaris, sitting, standing, laying | 1. raw data in the format (1:6) are from  two files:  y_test.txt and y_train.text; activity_labes.txt shows the descriptive activity names2. merge data in the step #13. In the Step #3, replace the 1:6 with activity names by using mapvalues in plyr package                                                                                                                                                                                                                                    |
| tBodyAcc-mean()-X           | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyAcc-mean()-Y           | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyAcc-mean()-Z           | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyAcc-std()-X            | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyAcc-std()-Y            | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyAcc-std()-Z            | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tGravityAcc-mean()-X        | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tGravityAcc-mean()-Y        | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tGravityAcc-mean()-Z        | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tGravityAcc-std()-X         | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tGravityAcc-std()-Y         | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tGravityAcc-std()-Z         | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyAccJerk-mean()-X       | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyAccJerk-mean()-Y       | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyAccJerk-mean()-Z       | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyAccJerk-std()-X        | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyAccJerk-std()-Y        | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyAccJerk-std()-Z        | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyGyro-mean()-X          | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyGyro-mean()-Y          | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyGyro-mean()-Z          | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyGyro-std()-X           | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyGyro-std()-Y           | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyGyro-std()-Z           | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyGyroJerk-mean()-X      | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyGyroJerk-mean()-Y      | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyGyroJerk-mean()-Z      | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyGyroJerk-std()-X       | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyGyroJerk-std()-Y       | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyGyroJerk-std()-Z       | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyAccMag-mean()          | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyAccMag-std()           | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tGravityAccMag-mean()       | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tGravityAccMag-std()        | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyAccJerkMag-mean()      | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyAccJerkMag-std()       | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyGyroMag-mean()         | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyGyroMag-std()          | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyGyroJerkMag-mean()     | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| tBodyGyroJerkMag-std()      | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| fBodyAcc-mean()-X           | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| fBodyAcc-mean()-Y           | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| fBodyAcc-mean()-Z           | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| fBodyAcc-std()-X            | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| fBodyAcc-std()-Y            | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| fBodyAcc-std()-Z            | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| fBodyAccJerk-mean()-X       | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| fBodyAccJerk-mean()-Y       | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| fBodyAccJerk-mean()-Z       | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| fBodyAccJerk-std()-X        | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| fBodyAccJerk-std()-Y        | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| fBodyAccJerk-std()-Z        | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| fBodyGyro-mean()-X          | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| fBodyGyro-mean()-Y          | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| fBodyGyro-mean()-Z          | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| fBodyGyro-std()-X           | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| fBodyGyro-std()-Y           | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| fBodyGyro-std()-Z           | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| fBodyAccMag-mean()          | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| fBodyAccMag-std()           | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| fBodyBodyAccJerkMag-mean()  | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| fBodyBodyAccJerkMag-std()   | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| fBodyBodyGyroMag-mean()     | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| fBodyBodyGyroMag-std()      | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| fBodyBodyGyroJerkMag-mean() | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
| fBodyBodyGyroJerkMag-std()  | the average of this variable for the activity and the subject showed on the same row; | numeric variable;                                                                           | 1. raw data are from  two files:  x_test.txt and x_train.text; 2. merge data in the step #1. Also, in the step #1, the descrpitve  name was added to the variable. The name is from the file, features.txt3. in the step #2, extract only the measurements on the mean and standard deviation for each measurement, that is the variable name with only mean() and std().4. in the step #5, group ID and activity first and then calculate the average of each variable by using aggregate commend |
